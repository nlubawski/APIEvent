# APIEvent

Para executar crie dentro do projeto  APIEvent um arquivo appsettings.json com a configuração de banco de dados e chave pra criação to token JWT, como ilustrado:

{
  "ConnectionStrings": {
    "DefaultConnection": "sua connection string aqui"
  },
  "secretKey": "seu token JWT aqui"
}
